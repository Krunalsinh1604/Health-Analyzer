"""
Entry point for the Health Analyzer backend.

Run from the `backend/` directory:
    uvicorn src.api:app --reload

Or use the helper scripts in the project root:
    run_backend.ps1   (PowerShell)
    run_backend.bat   (CMD / double-click)
"""

# This file exists so that IDEs can discover the package root.
# The actual FastAPI app lives in src/api.py.
